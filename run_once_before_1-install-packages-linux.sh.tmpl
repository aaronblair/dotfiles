{{- if eq .chezmoi.os "linux" -}}
#!/bin/sh
set -e

echo "[chezmoi] Installing packages for Linux..."

# Determine if we need sudo
if [ "$(id -u)" -eq 0 ]; then
    SUDO=""
else
    SUDO="sudo"
fi

if command -v apk > /dev/null 2>&1; then
    echo "[chezmoi] Detected Alpine (apk)"
    apk add --no-cache \
        curl eza fzf zoxide neovim ripgrep fd lazygit delta htop tmux git zsh zellij \
        || apk add --no-cache \
            curl eza fzf zoxide neovim ripgrep fd lazygit delta htop tmux git zsh

elif command -v apt-get > /dev/null 2>&1; then
    echo "[chezmoi] Detected Debian/Ubuntu (apt)"
    $SUDO apt-get update

    APT_PACKAGES="curl eza fzf zoxide neovim ripgrep fd-find htop tmux git zsh"
    if apt-cache show zellij > /dev/null 2>&1; then
        APT_PACKAGES="$APT_PACKAGES zellij"
    else
        echo "[chezmoi] Note: zellij not available in apt; will install latest release if needed"
    fi

    $SUDO apt-get install -y \
        $APT_PACKAGES

    # lazygit and delta may need manual install on older Ubuntu
    command -v lazygit > /dev/null 2>&1 || echo "[chezmoi] Note: lazygit not in apt"
    command -v delta > /dev/null 2>&1 || echo "[chezmoi] Note: delta not in apt"
fi

if ! command -v zellij > /dev/null 2>&1; then
    echo "[chezmoi] Installing latest Zellij release to ~/.local/bin..."

    case "$(uname -m)" in
        x86_64|amd64) ZELLIJ_ARCH="x86_64-unknown-linux-musl" ;;
        aarch64|arm64) ZELLIJ_ARCH="aarch64-unknown-linux-musl" ;;
        *)
            echo "[chezmoi] Unsupported architecture for Zellij binary: $(uname -m)"
            exit 0
            ;;
    esac

    mkdir -p "$HOME/.local/bin"
    TMPDIR="$(mktemp -d)"
    trap 'rm -rf "$TMPDIR"' EXIT HUP INT TERM

    URL="https://github.com/zellij-org/zellij/releases/latest/download/zellij-${ZELLIJ_ARCH}.tar.gz"
    if command -v curl > /dev/null 2>&1; then
        curl -fsSL "$URL" -o "$TMPDIR/zellij.tar.gz"
    else
        wget -qO "$TMPDIR/zellij.tar.gz" "$URL"
    fi

    tar -xzf "$TMPDIR/zellij.tar.gz" -C "$TMPDIR"
    install -m 0755 "$TMPDIR/zellij" "$HOME/.local/bin/zellij"
fi

echo "[chezmoi] Linux package installation complete"
{{- end -}}
